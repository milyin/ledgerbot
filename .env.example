# Environment variables template for ledgerbot
# Copy this file to .env and fill in your actual values

# Deployment Configuration
# SSH user for deployment
DEPLOY_USER=your_username

# Deployment host/server
DEPLOY_HOST=your_server.example.com

# Path on the remote server where the binary should be deployed (debug mode)
DEPLOY_PATH=/path/to/deployment/directory

# Path on the remote server where the binary should be deployed (release mode)
DEPLOY_PATH_RELEASE=/path/to/deployment/directory/release

# Predefined Telegram Bot Token
# PREDEFINED_BOT_TOKEN_RELEASE=your_telegram_bot_token
# PREDEFINED_BOT_TOKEN_DEBUG=your_telegram_bot_token

# Additional parameters for cargo build
# CARGO_BUILD_FLAGS=--release